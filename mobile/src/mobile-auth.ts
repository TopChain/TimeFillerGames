import {createClient,type Session,type SupabaseClient} from '@supabase/supabase-js';
let client:SupabaseClient|null=null;const url=import.meta.env.VITE_SUPABASE_URL??'',key=import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY??'';
export function hasBrowserSupabaseConfig(){return Boolean(url&&key);}export function getBrowserSupabase(){if(!client)client=createClient(url,key,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:false}});return client;}
export async function currentSession():Promise<Session|null>{if(!hasBrowserSupabaseConfig())return null;const{data,error}=await getBrowserSupabase().auth.getSession();if(error)throw error;return data.session;}
export async function ensureParticipantSession(){const existing=await currentSession();if(existing)return existing;const{data,error}=await getBrowserSupabase().auth.signInAnonymously();if(error||!data.session)throw new Error(error?.message??'Could not create a temporary guest session.');return data.session;}
export async function requestHostMagicLink(email:string){const redirect=import.meta.env.VITE_AUTH_REDIRECT_URL||'timefillergames://auth/callback';const{error}=await getBrowserSupabase().auth.signInWithOtp({email:email.trim(),options:{emailRedirectTo:redirect}});if(error)throw error;}
export async function permanentHostSession(){const s=await currentSession();return !s||s.user.is_anonymous?null:s;}
export async function importNativeAuthUrl(value:string){const parsed=new URL(value),code=parsed.searchParams.get('code');if(code){const{data,error}=await getBrowserSupabase().auth.exchangeCodeForSession(code);if(error)throw error;return data.session;}const hash=new URLSearchParams(parsed.hash.replace(/^#/,'')),access=hash.get('access_token'),refresh=hash.get('refresh_token');if(!access||!refresh)return null;const{data,error}=await getBrowserSupabase().auth.setSession({access_token:access,refresh_token:refresh});if(error)throw error;return data.session;}
